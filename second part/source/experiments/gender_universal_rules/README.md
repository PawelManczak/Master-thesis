# Gender & Demographics Universal Rules

This folder explores differences in the generation and stability of ARMADA rules across demographic groups. The scripts specifically analyze variations between genders (`gender_universal_rules.py`) and age groups (`age_universal_rules.py`). The objective is to verify the universality of physiological connections independent of these demographic characteristics.
